# CareerBook — Coming Soon

The region's first AI-powered hiring platform. Launching soon.

## Development

This is a monorepo containing:
- `apps/frontend`: Vite + React + TypeScript + Tailwind CSS
- `apps/backend`: Express + TypeScript + Prisma + PostgreSQL

### Running Locally

Run `npm run dev` from the root directory to start both the frontend and backend.
